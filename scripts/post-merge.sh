#!/bin/bash
set -e

# Post-merge setup for Wither Launcher (Android APK project)
# No package installs or migrations needed — this is a pure Android/Gradle project.
# The Gradle wrapper and all dependencies are managed by Gradle itself at build time.

echo "Post-merge setup complete."
